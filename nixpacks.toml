[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start.web]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[start.worker]
cmd = "python worker.py"
